services:
  api:
    container_name: vps-vnode-rag-api-labs
    restart: unless-stopped
    networks:
      default:
        aliases:
          - vnode-rag-api-labs

networks:
  default:
    name: vps_network
    external: true
